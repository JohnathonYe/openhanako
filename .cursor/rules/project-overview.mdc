---
description: OpenHanako 项目结构、技术栈与模块职责
alwaysApply: true
---

# OpenHanako 项目概览

## 架构

- **core/** — 引擎编排层 + Manager（HanaEngine 为 Thin Facade，委托 AgentManager / SessionCoordinator / ConfigCoordinator / ChannelManager / ModelManager / PreferencesManager / SkillManager）
- **lib/** — 核心库：memory、tools、sandbox、bridge、desk、channels、llm、oauth 等
- **server/** — Fastify 5 HTTP + WebSocket，路由在 `server/routes/*.js`
- **hub/** — 调度器、频道路由、事件总线（独立于当前会话）
- **desktop/** — Electron 38 主进程；前端在 `desktop/src`（React 19 + Vite 7 渐进迁移中，部分仍为旧 JS/CSS）
- **skills2set/** — 内置技能定义（SKILL.md + 资源）
- **tests/** — Vitest 测试

## 技术栈

| 层级 | 技术 |
|------|------|
| 桌面 | Electron 38 |
| 前端 | React 19 + Vite 7，路径别名 `@/` → `desktop/src/react` |
| 服务端 | Fastify 5，ESM |
| 后端核心 | Node ≥20，ESM（core/lib/server 均为 .js） |
| 数据库 | better-sqlite3（WAL） |
| 测试 | Vitest |

## 常用命令

- `npm start` — 构建前端 + 启动 Electron
- `npm run start:vite` — Vite HMR 开发
- `npm run typecheck` — TypeScript 检查（仅覆盖 desktop/src）
- `npm test` — Vitest 运行

## 调试环境变量

- `HANA_DEBUG_LLM_PAYLOAD=1` — 在终端摘要打印发往 LLM 的 **openai-completions** 请求 `messages` 结构（`image_url` 仅显示 mime 与 base64 长度）。与 `npm start` 同用。
- `HANA_DEBUG_CHANNEL_FORCE_REPLY=1` — 频道 **仅本地调试**：跳过 triage 的 YES/NO，并用简短单轮提示生成回复（仍走主会话模型）；若仍失败则回退一句固定话。用于验证「频道能出字」。

## 排查日志（频道）

- 持久化：`~/.hanako/logs/*.log`（每次启动一个新文件）
- `[api]`：`POST /channels/.../messages` 含发送者、时间戳、正文长度与 **preview**（截断）、`@` 解析与 `triggerChannelTriage` 目标
- `[channel]`：调度器是否启动、`triggerImmediate`、`_executeCheck` 的上下文 **contextPreview**、triage/回复结果
- `[ticker]`：立即 triage 队列、agent 被跳过原因（无 bookmark / 窗口为空）、`getAgentOrder()` 为空

## 约定

- 后端（core/lib/server）使用 **ESM**（`import`/`export`），文件扩展名 `.js`
- 前端 React 代码在 `desktop/src/react`，使用 **TypeScript**（`.ts`/`.tsx`）
- 用户数据目录：`~/.hanako`，可通过环境变量 `HANA_HOME` 覆盖
